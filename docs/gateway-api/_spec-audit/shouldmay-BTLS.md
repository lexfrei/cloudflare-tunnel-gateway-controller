# BackendTLSPolicy (BTLS-*) + Policy Attachment (POL-*) + GEP-* — SHOULD/MAY audit

Gateway API v1.5.1 (Standard channel). Architecture: in-process L7 proxy owns backend connections, so backend TLS lives in the proxy transport. `GatewayClassConfig` is the controller's OWN CRD (not a Gateway API policy); POL-* PolicyAncestorStatus clauses are evaluated against BackendTLSPolicy status. Conformance green this cycle (BackendTLSPolicy sub-tests included; HTTPS-listener parent test is edge-TLS N/A).

Verified against: `internal/controller/backendtlspolicy_controller.go`, `internal/controller/proxy_syncer.go`, `internal/proxy/handler.go`, `docs/gateway-api/limitations.md`.

## SHOULD / SHOULD NOT clauses

| ID | Keyword | Verdict | Evidence | Notes |
| --- | --- | --- | --- | --- |
| BTLS-04 | SHOULD NOT support more than one targetRef | DEVIATED-DOCUMENTED | limitations.md:164 "Multiple targetRefs per policy: Yes"; proxy_syncer.go:427 ranges policy.Spec.TargetRefs; backendtlspolicy_controller.go:590 ranges TargetRefs | Controller deliberately supports >1 targetRef (all targeted Services share one TLS config). CRD CEL (vendor backendtlspolicy_types.go) only enforces sectionName uniqueness, not single-ref. Deviation is documented + conflict resolution per (Service,SectionName) covers the multi-ref status gap the spec warned about. First-pass marked this NA — corrected to DEVIATED-DOCUMENTED. |
| BTLS-06 | Consistent behaviour outside extended HTTPRoute->Service | HONOURED-TESTED (TestConvertRoutes_BackendTLSEquivalentAcrossRouteKinds) | proxy_syncer.go:282 newBackendTLSResolver; gatewaysForPolicy:580 + GRPCRoute lists | Behaviour identical for HTTPRoute->Service AND GRPCRoute->Service (both walked in gatewaysForPolicy / collectGatewayKeys). Mesh / non-Route / Gateway-feature backends are out of scope (controller only resolves Service backends reachable via HTTP/GRPC routes), so "consistency outside HTTPRoute" is satisfied for the one extra path we support; the GRPCRoute-vs-HTTPRoute equivalence is pinned by TestConvertRoutes_BackendTLSEquivalentAcrossRouteKinds (identical resolver key and TLS material from both converters). |
| BTLS-07 | Document interpretation in those scenarios | HONOURED-TESTED | limitations.md:151-170 "Backend mTLS (BackendTLSPolicy)" scope table | Full scope table documents supported resources (Service only), CA source, SAN modes, WellKnown rejection, conflict resolution, multi-targetRef, SectionName. Conformance run exercises the documented behaviour. |
| POL-01 | Policy discoverability label SHOULD be one of listed values | N-A | no gateway.networking.k8s.io/policy label set anywhere (grep internal/ api/ = no hits) | Conditional SHOULD: only applies IF the implementation sets the discoverability label. Controller sets no policy label, so the value-constraint never engages. Not a deviation — the label itself is optional. |
| POL-03 | Gateway as PolicyAncestorStatus object | HONOURED-TESTED | backendtlspolicy_controller.go:772 gatewayAncestorRef (Kind="Gateway"); updateStatus:745 stamps it | Every ancestor entry's AncestorRef is the managed Gateway. TestReconcile_HappyPath / GRPCRouteAttachment_PopulatesAncestor cover it. |
| POL-04 | PolicyAncestorStatus struct still used for parent-as-status | HONOURED-TESTED | updateStatus:745 PolicyAncestorStatus{} | Struct used uniformly; same tests as POL-03. |
| POL-13 | Update ancestor entry when relevant ancestor modified | HONOURED-TESTED | SetupWithManager:839 watches BackendTLSPolicy(peer) + HTTPRoute + GRPCRoute + ConfigMap; Reconcile:233 recomputes gatewaysForPolicy each pass | Ancestor set + conditions recomputed from scratch on every reconcile, and watches fire on route/CM/peer changes that alter the ancestor set or CA validity. TestReconcile_ConflictResolution / peer-deletion flip covers re-evaluation. |
| POL-11 | MUST NOT overwrite Condition whose observedGeneration exceeds ours | DEVIATED-SILENT | updateStatus:742 meta.SetStatusCondition; no >-skip guard anywhere | (Listed in inventory as MUST NOT; included for completeness.) No code refuses to overwrite a Condition whose stored observedGeneration is greater than policy.Generation. Single-writer-per-controllerName + RetryOnConflict(706) makes a stale overwrite improbable, but the explicit guard the clause mandates is absent. Not documented. |
| POL-18 | MUST signal unimplementable when ancestor slice full | DEVIATED-SILENT | updateStatus:756-760 truncates OUR entries at 16, no condition/event signalling unimplementable | (MUST, included for completeness.) Overflow past 16 ancestors silently drops our excess entries; policy is NOT marked unimplementable nor signalled on related resources. Realistic single-Gateway accounts never hit 16 (comment controller.go:31-39), but the signal the clause mandates is missing + undocumented. |
| GEP-05 | PolicyAncestorStatus struct, composite-key distinct | HONOURED-TESTED | updateStatus:728 ancestorRefKey + ControllerName filter; cap test TestUpdateStatus_PreservesOtherControllersUnderCap | Entries distinct by (AncestorRef, ControllerName). Overlaps POL-12..16. |
| GEP-07 | Rejected policy gets Accepted=False | HONOURED-TESTED | conflictedConditions:309 / caInvalidConditions:451 / wellKnownUnsupportedConditions:482 all Accepted=False | Every rejection path stamps Accepted=False with a specific Reason. Conformance + unit tests cover Conflicted / NoValidCACertificate / Invalid. |
| GEP-08 | Discoverability condition on affected objects | DEVIATED-DOCUMENTED | ancestor condition written only on the BackendTLSPolicy.Status.Ancestors; rationale in limitations.md ("Policy discoverability conditions stay on the policy") | The discoverability hint lives on the policy's own ancestor status, never on the affected Service/Gateway. Documented deviation: Services are user-owned objects whose status the controller never writes, and the ancestor entries already name every affected Gateway. |

## MAY clauses

| ID | Verdict | Evidence | Notes |
| --- | --- | --- | --- |
| BTLS-05 | OMITTED-INTENTIONAL | gatewaysForPolicy:590 only matches Kind=="Service" targets reachable via HTTP/GRPC routes | BackendTLSPolicy for non-Route Services / Gateway-feature backends / mesh / other kinds is NOT implemented; scope limited to Service backends used by routes. Documented (limitations.md:164 + scope table). |
| BTLS-08 | N-A | — | Future common Options keys; API has not defined any. Nothing to implement. |
| BTLS-13 | IMPLEMENTED | backendtlspolicy_controller.go:56 parseCABundle (PEM decode + x509.ParseCertificate); TestParseCABundle_RejectsMalformedCert | Optional further cert-content validation IS performed: bundle must contain >=1 parseable CERTIFICATE block or the policy is rejected. |
| BTLS-17 | IMPLEMENTED | proxy_syncer.go:500 loop concatenates every CACertificateRef into one pool; handler.go:866 AppendCertsFromPEM; limitations.md:159 | Multiple CA certs attached to a backend (all refs concatenated into the trust pool). Implementation-specific behaviour exercised. |
| BTLS-21 | OMITTED-INTENTIONAL | computeConditions:288 + wellKnownUnsupportedConditions:482 reject any WellKnownCACertificates value as Accepted=False/Invalid | No custom WellKnownCACertificates set defined; only explicit CACertificateRefs honoured. Documented (limitations.md:163). |

## Corrections to first-pass (rows-BTLS.md)

- BTLS-04: first-pass = NA. Correct verdict = DEVIATED-DOCUMENTED — the controller actively supports >1 targetRef (proxy_syncer.go:427, controller.go:590) AND documents it (limitations.md:164), directly opposing the SHOULD NOT. Not a no-op "advisory" — it is an exercised, documented deviation.
