# GR + SH SHOULD/MAY tier audit (Gateway API v1.5.1)

Adversarial verification of the SHOULD/SHOULD-NOT and MAY clauses for GRPCRoute (GR-) and shared types (SH-). Verified against `internal/proxy/{grpc_converter,converter,matcher,handler}.go`, `internal/controller/{grpcroute_controller,route_status}.go`, `internal/routebinding/`, `internal/ingress/`, cross-referenced with `test/conformance/conformance_test.go` and `docs/gateway-api/limitations.md`.

Verdicts: HONOURED-TESTED / HONOURED-UNTESTED / DEVIATED-DOCUMENTED / DEVIATED-SILENT / N-A.

## SHOULD / SHOULD NOT

| ID | Keyword | Verdict | Evidence | Notes |
| --- | --- | --- | --- | --- |
| GR-44 | honor Service appProtocol (SHOULD) | DEVIATED-SILENT | internal/proxy/grpc_converter.go:289-360, applyGRPCBackendTransport:402-425 | gRPC converter forces h2c and consults only the BackendTLSPolicy resolver; the Service-port appProtocol field is never read. For appProtocol=kubernetes.io/h2c or unset the forced-h2c result coincidentally matches intent, but a gRPC backend declaring a TLS appProtocol (https / kubernetes.io/wss) with no BackendTLSPolicy is silently dialed cleartext h2c — no diagnostic, no fail-closed (the HTTP path at converter.go:1290 fails such backends closed + ResolvedRefs=False). Only documented as a dev note "forces h2c upstream" (architecture.md:180, grpcroute_controller.go:398); no user-facing limitations.md entry and no route condition surfaces the ignored field. The MUST-pass N-A verdict ("gRPC is HTTP/2 by definition") holds only for transport family — appProtocol still selects TLS-vs-cleartext, which is dropped silently. |
| GR-45 | recognize KEP-3726 standard protocols (SHOULD) | DEVIATED-SILENT | internal/controller/grpcroute_controller.go:398; internal/proxy/grpc_converter.go:289 | Conditional on GR-44 ("supporting appProtocol"). The gRPC path recognizes no KEP-3726 value — h2c is unconditional, and kubernetes.io/wss / https on a gRPC backend are ignored rather than recognized. Same silent-drop surface as GR-44; no diagnostic. The HTTP path (SH-27) does recognize them, so the capability exists in-tree but is not wired into the gRPC converter. |
| SH-26 | honor Service appProtocol (SHOULD) | HONOURED-TESTED | internal/proxy/converter.go:1080-1095,1290-1350 (resolveBackendProtocol) | HTTP path reads the Service port appProtocol and maps kubernetes.io/h2c, ws, wss, https to transport selection; TLS values without a BackendTLSPolicy fail closed with ResolvedRefs=False/UnsupportedProtocol. Exercised by conformance SupportHTTPRouteBackendProtocolH2C (conformance_test.go:81) and e2e backend-protocol-websocket. |
| SH-27 | recognize KEP-3726 standard protocols (SHOULD) | HONOURED-TESTED | internal/proxy/converter.go:42-58 (appProtocolH2C/WS/WSS consts) | Recognizes kubernetes.io/h2c, kubernetes.io/ws, kubernetes.io/wss (KEP-3726) plus https; documented in limitations.md:107-117. h2c covered by conformance H2C test; ws/wss by e2e. |
| SH-19 | parentRefs must be distinct (must, lc) | N-A | internal/proxy/converter.go (no de-dup); shared_types.go:199 | Non-normative lowercase prose, no CEL marker in the vendored CRD; controller does not de-dupe and is not required to. |
| SH-31 | prefer standard Accepted reasons (should, lc) | HONOURED-UNTESTED | internal/controller/route_status.go:345-364 | Accepted condition uses only standard RouteReason* values (Accepted / Pending / NoMatchingParent / NoMatchingListenerHostname / Conflicted). No custom reasons. Not directly conformance-asserted. |
| SH-32 | prefer standard ResolvedRefs reasons (should, lc) | HONOURED-UNTESTED | internal/controller/route_status.go:382-413 | ResolvedRefs uses only standard reasons (ResolvedRefs / RefNotPermitted / InvalidKind / BackendNotFound / UnsupportedProtocol). |
| SH-38 | Fall Back only with restart-state restore (should, lc) | N-A | internal/controller/route_status.go:251-294 | Non-normative; the controller chose the Dropped-Rule approach, not Fall Back / last-known-good, so the restart-state-restore guidance does not apply. |
| SH-43 | clean up stale own-controllerName entries (should, lc) | DEVIATED-DOCUMENTED | internal/controller/route_status.go:112 (Parents=nil rebuild) | Lowercase should. The blanket Parents=nil rebuild re-adds only currently-managed refs, so own entries for refs dropped from spec are incidentally cleaned; but there is no finalizer-based cleanup of own entries when the route still references a now-unmanaged parent. Behavior is captured in the SH-47/SH-56/SH-57 audit (same code path, rows-SH.md). |
| SH-77 | avoid reusing session names (should, lc) | N-A | grep SessionPersistence -> none | SessionPersistence is unimplemented (no controller/proxy surface); the clause is user-facing guidance, vacuously satisfied. |

## MAY

| ID | Verdict | Evidence | Notes |
| --- | --- | --- | --- |
| GR-04 | OMITTED-INTENTIONAL | docs/gateway-api/limitations.md:96; internal/tunnel/origin.go | HTTP/1->HTTP/2 upgrade for HTTPS listeners (ALPN) is edge-owned by Cloudflare; the proxy receives requests over the tunnel from cloudflared, not from raw clients, so there is no client-facing HTTP/1 connection to upgrade. Optional clause; not part of this data plane. |
| GR-07 | OMITTED-INTENTIONAL | docs/gateway-api/limitations.md:96; internal/tunnel/origin.go | Same as GR-04 for the HTTP (h2c) listener type: HTTP/1->HTTP/2 upgrade-without-prior-knowledge is edge-owned. The proxy serves gRPC as HTTP/2 over the tunnel; no client-facing upgrade negotiation here. |
| GR-46 | IMPLEMENTED | internal/proxy/grpc_converter.go:289-296,402-425; convertGRPCExternalBackendRef:374-396 | When Service appProtocol is unset the gRPC converter infers the backend protocol by its own means: h2c by default, and HTTP/2-over-TLS (ALPN) when a BackendTLSPolicy targets the Service or an ExternalBackend uses the https scheme. This is the MAY-permitted own-inference. (Note: the inference IGNORES a set appProtocol rather than honoring it — that is the GR-44 SHOULD deviation; the MAY only covers the unset case, which is satisfied.) |
| SH-28 | IMPLEMENTED | internal/proxy/converter.go:1333 (default HTTP/1.1 fallback) | HTTP path: when appProtocol is unset the proxy infers HTTP/1.1 as the backend protocol — own-means inference, MAY-permitted. |
| SH-29 | N-A | — | No clause SH-29 exists in 01-clause-inventory.md (SH BackendRef MAY rows are SH-28 only; the row after SH-28 is SH-30 MUST). The task brief's "SH-29" maps to the single SH-28 appProtocol-inference MAY, catalogued above. |
