# HTTPRoute SHOULD / SHOULD NOT / MAY audit (HR-*)

Audited against vendored `sigs.k8s.io/gateway-api v1.5.1` (Standard channel) and the in-process L7 proxy data plane. All L7 routing/matching/filtering/timeout/error semantics live in `internal/proxy` + `internal/ingress` + `internal/controller`, NOT in Cloudflare Tunnel — the vendored cloudflared fork's `OverrideProxy` hook funnels all traffic through `internal/tunnel/origin.go` → the proxy. Verdicts derived by reading the code, not by trusting the first-pass `rows-HR.md` hint (corrections noted at the bottom).

## SHOULD / SHOULD NOT clauses (13)

| ID | Keyword | Verdict | Evidence | Notes |
| --- | --- | --- | --- | --- |
| HR-09 | SHOULD | HONOURED-TESTED | internal/proxy/converter.go:258 (compile in spec order) + filter.go:597 ApplyRequestFilters (`for _, filter := range filters`) + filter.go:609 ApplyResponseFilters; integration_test.go:2193 TestHandler_MultipleFiltersApplied | Filters compiled and applied in the order specified; request pipeline then response pipeline. Slice order is preserved end to end. |
| HR-12 | SHOULD | N-A | vendor httproute_types.go:224 (conditional on strict-ordering rejection) | "To reject an invalid combination" is conditioned on choosing the strict interpretation of HR-10. The proxy does NOT reject combinations; it applies in order. The SHOULD therefore never triggers. |
| HR-18 | SHOULD | HONOURED-TESTED | internal/controller/zero_endpoint_backends.go:34 markZeroEndpointBackends + proxy_syncer.go:774 + mark_unavailable.go:21 (status=503); zero_endpoint_backends_test.go:124 MarksServiceWithNoReadyEndpoints; docs/gateway-api/limitations.md:66 | Service with zero ready endpoints marked 503 per traffic fraction (weighted pool), controller watches EndpointSlices (httproute_controller.go:307) so marking refreshes. 500-invalid-ref wins over 503 on conflict (mark_unavailable.go:17). |
| HR-21 | SHOULD | HONOURED-TESTED (TestConvertTimeouts explicit-zero rows + TestRuleHeaderTimeout_ExplicitZeroDisables) | internal/proxy/converter.go:1539 convertTimeouts (`time.ParseDuration("0s")` → 0) + handler.go:203 ruleHeaderTimeout (0 → unbounded ResponseHeaderTimeout) | `0s` parses to `time.Duration(0)`, stored identically to "unset", and ruleHeaderTimeout returns 0 → stdlib leaves ResponseHeaderTimeout unbounded = timeout disabled, which is what the SHOULD asks. BUT explicit `0s` is indistinguishable from "unset" — compliant by collapse, not by an explicit disable-signal. TestConvertTimeouts now carries explicit-`0s` rows and TestRuleHeaderTimeout_ExplicitZeroDisables pins the zero-means-unbounded handler semantic. |
| HR-24 | SHOULD | HONOURED-TESTED (TestConvertTimeouts explicit-zero rows + TestRuleHeaderTimeout_ExplicitZeroDisables) | internal/proxy/converter.go:1551 convertTimeouts (BackendRequest `0s` → 0) + handler.go:203 ruleHeaderTimeout | Same mechanism as HR-21: BackendRequest `0s` collapses to 0 → unbounded. Both knobs share one header-only timeout (limitations.md:135). Compliant by collapse; the explicit zero-disable semantic is pinned by the same tests as HR-21. |
| HR-26 | SHOULD | N-A | vendor httproute_types.go:314 `<gateway:experimental>` Retry field; no retry code in internal/proxy (handler.go:189 godoc "this proxy has no retry logic") | HTTPRouteRetry is experimental-channel only; the shipped Standard CRD has no retry field and the proxy never retries (the only `retry` in the tree is RequestMirror dispatch, filter.go:482). "retry on connection errors if a retry stanza is configured" cannot trigger — no stanza is ever configurable. |
| HR-32 | SHOULD | N-A | vendor httproute_types.go:~427 (Retry experimental); handler.go:189 | "Cancel in-progress retries when Request timeout reached" is inapplicable — no retries exist. The Request timeout itself returns 504 immediately (handler.go:1040 errorHandler). |
| HR-34 | SHOULD | N-A | vendor httproute_types.go:~432 (Retry experimental); handler.go:189 | "Cancel retries reaching BackendRequest timeout" inapplicable — no retries. |
| HR-45 | SHOULD NOT | N-A | vendor httproute_types.go:694 (user-facing guidance) | "Users SHOULD NOT route on repeated query params" addresses the USER, not the implementation. No controller obligation. (Behaviourally the matcher in matcher.go:131 does `slices.Contains(r.URL.Query()[name], value)`, i.e. matches if ANY repeated value matches — a defensible, documented choice, but the clause imposes nothing on us.) |
| HR-61 | SHOULD | DEVIATED-DOCUMENTED | internal/proxy/filter.go:185 buildRedirectBase (port added only when `config.Port != nil`, no listener-port fallback); controller/route_redirect_scheme.go defaults the SCHEME only, never a port | "If the redirect scheme has no well-known port, the listener port SHOULD be used." The controller resolves the scheme from the accepted listener but never stamps the listener PORT into the redirect. Behind the tunnel there is no real listener port (edge uses 80/443), so this is an architectural deviation — now documented in limitations.md ("Redirect port defaulting never needs the listener fallback"). Practically harmless: the http/https enum (route_redirect_scheme.go:14) both HAVE well-known ports, so the "no well-known port" branch is unreachable for the only two schemes the CRD permits. |
| HR-63 | SHOULD NOT | HONOURED-TESTED (TestRequestRedirect_NoPortInLocationWhenPortUnset) | internal/proxy/filter.go:209-213 (port appended to Host only when `config.Port != nil`) | No port is added to the Location host unless the filter explicitly sets Port; for http/80 and https/443 with Port omitted the default path emits no port, satisfying the SHOULD NOT. (Spec also says don't add port when it equals scheme default even if specified — that narrow sub-case is not special-cased — an explicitly set Port=80/443 is echoed even though it matches the scheme default; minor deliberate non-handling, not a test gap.) |
| HR-67 | SHOULD | HONOURED-TESTED | internal/proxy/converter.go:1290 resolveBackendProtocol (reads Service port appProtocol via BackendProtocolResolver, converter.go:66); converter_test.go:233 BackendProtocolH2C, appprotocol_failclosed_test.go:42; limitations.md:107 | Service port appProtocol honoured: h2c/ws/wss/https mapped to the upstream transport; TLS hints without BackendTLSPolicy fail closed (502, ResolvedRefs=False). |
| HR-68 | SHOULD | HONOURED-TESTED | internal/proxy/converter.go:42-58 (consts kubernetes.io/h2c, /ws, /wss); converter_test.go:233, appprotocol_failclosed_test.go:51 | Recognises the KEP-3726 Kubernetes Standard Application Protocols (kubernetes.io/h2c, kubernetes.io/ws, kubernetes.io/wss). |

## MAY clauses (7)

| ID | Verdict | Evidence | Notes |
| --- | --- | --- | --- |
| HR-10 | OMITTED-INTENTIONAL | internal/proxy/filter.go:597 (applies in order, never rejects) | Strict-ordering rejection of unsupported filter combinations is not implemented; the proxy applies filters in spec order rather than rejecting. URLRewrite+RequestRedirect on one rule is the only hard-rejected combo, enforced at admission by CRD CEL (httproute_types.go:252), not by this MAY. No documentation obligation (HR-11) since strict interpretation is not chosen. |
| HR-23 | IMPLEMENTED | internal/proxy/handler.go:185-202 ruleHeaderTimeout godoc; limitations.md:141 | Permitted MAY taken: the timeout is measured as a header-only deadline (stdlib ResponseHeaderTimeout), which per godoc starts only AFTER the request body is fully written — i.e. "after the entire request stream has been received". Explicitly documented as a deliberate interpretation. |
| HR-28 | OMITTED-INTENTIONAL | vendor httproute_types.go:417 (Retry.Backoff experimental); handler.go:189 | Retry backoff strategy inapplicable — no retry support (experimental field, not in Standard CRD). |
| HR-35 | OMITTED-INTENTIONAL | vendor httproute_types.go:~437 (Retry experimental); handler.go:189 | "Retry timeout default when no BackendRequest timeout" inapplicable — no retries. |
| HR-38 | OMITTED-INTENTIONAL | vendor httproute_types.go:460 HTTPRouteRetryStatusCode `<gateway:experimental>`; handler.go:189 | Additional retriable 500-599 status codes inapplicable — no retry support. |
| HR-39 | OMITTED-INTENTIONAL | vendor httproute_types.go:463 (HTTPRouteRetryStatusCode experimental); handler.go:189 | Retriable 400-499 status codes inapplicable — no retry support. |
| HR-69 | IMPLEMENTED | internal/proxy/converter.go:1329-1351 resolveBackendProtocol default branch; converter_test.go:276 UnknownAppProtocol_LogsAndDefaults, :233 default path | MAY taken: absent appProtocol infers HTTP/1.1; unrecognised values fall back to HTTP/1.1 (report-only diagnostic, limitations.md:25). |

## First-pass corrections vs rows-HR.md

- HR-21 / HR-24: rows-HR labelled "PARTIAL". The actual mechanism is HONOURED (0s does disable, via collapse to unset) — verdict upgraded to HONOURED-TESTED once the explicit disable-signal tests landed (TestConvertTimeouts zero rows + TestRuleHeaderTimeout_ExplicitZeroDisables).
- HR-61: rows-HR marked "GAP"; since documented in limitations.md the verdict is DEVIATED-DOCUMENTED, and it is materially mitigated: the CRD scheme enum is only http/https, both of which HAVE well-known ports, so the "no well-known port" branch is unreachable for any admitted value. The deviation is real but not reachable through the Standard API.
- Line numbers in rows-HR.md are stale: the timeout helper it cites as `handler.go:203-218 ruleHeaderTimeout` is accurate for the helper, but the timeout PARSING is converter.go:1539 convertTimeouts, and the h2c synthetic header timeout is in the separate internal/proxy/headertimeout.go (not handler.go).
- HR-63: rows-HR cites filter.go:209-213 — correct.
